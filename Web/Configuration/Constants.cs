namespace Pinterest_Clone.Configuration
{
    public static class Constants
    {
        public const string DefaultConnection = nameof(DefaultConnection);
        public const string JwtConfiguration = nameof(JwtConfiguration);
    }
}
